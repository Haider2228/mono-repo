name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # Update to latest version

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3  # Update to latest version

    - name: Build and push Docker image
      uses: docker/build-push-action@v4  # Update to latest version
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ghcr.io/haider2228/mycppapp:latest  # Ensure repository name is lowercase

    - name: Logout from GitHub Container Registry
      run: docker logout ghcr.io

